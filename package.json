{
  "name": "app-setup-events-samples",
  "version": "1.0.0",
  "description": "NovaBridge MSP — Freshworks app setup events sample",
  "devDependencies": {
    "@vitest/coverage-v8": "~2.1.8",
    "vite": "~5.0.0",
    "vitest": "~2.1.8",
    "jsdom": "~26.1.0"
  },
  "scripts": {
    "fdk-unit-test": "vitest run --coverage"
  }
}
